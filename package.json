{
  "homepage": "https://buse.ilguz.github.io/beyazperdedeneme",
  "dependencies": {
    "mongoose": "^7.2.2"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts": {
    
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "start-client": "cd client && npm run start"
  }
  
}
